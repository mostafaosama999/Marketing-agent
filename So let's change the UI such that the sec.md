So let's change the UI such that the sections are not all expandable by default, and also let's add another column that shows with another feature.  
It shows essentially if I contacted them again, so the last contact date is fine, but I want to be able to add contacted again on another date that shows the difference between the first contact and the second contact. Maybe we can do something like we change the last contacted date to first contact, and then we can have another second contact date, something like that. 


